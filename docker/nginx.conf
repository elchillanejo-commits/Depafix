server {
    listen 80;
    location / { root /usr/share/nginx/html; index index.html; }
    location /api/ { proxy_pass http://fastapi:8000/; }
    location /v1/ { proxy_pass http://flask:5000/; }
}
