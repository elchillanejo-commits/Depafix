FROM python:3.12-slim
WORKDIR /app
RUN pip install flask
COPY core/servidor_depafix.py .
CMD ["python", "servidor_depafix.py"]
HEALTHCHECK --interval=30s CMD curl -f http://localhost:5000/health || exit 1
